# CLAUDE.md

ETRI 2026 자율주행 챌린지 · LAW_split 트랙.

## 공유 기록 규칙

이 프로젝트는 Claude와 Codex가 함께 작업한다. `SHARED_CONTEXT.md`가 두 에이전트의
공통 기억이다.

**작업 시작 전에 `SHARED_CONTEXT.md`를 읽는다.** 목표·확정 사항·측정된 수치·이미 배제한
대안·버그 이력이 거기 있다. 읽지 않고 시작하면 이미 실패한 방법을 다시 제안하거나
확정된 결정을 뒤집는 일이 생긴다.

**중요한 논의·결정·코드 변경·테스트를 마치면, 응답을 끝내기 전에 `SHARED_CONTEXT.md`를
갱신한다.** 다음에 해당하면 갱신 대상이다.
- 사용자가 목표나 제약을 새로 말했을 때
- 방법이나 설계를 확정했을 때, 또는 검토 후 배제했을 때
- 코드나 설정을 바꿨을 때
- 학습·평가를 실행해서 수치가 나왔을 때
- 버그를 찾았을 때 (증상·원인·대가를 함께 남긴다)

**기록할 때 출처를 구분한다.** 표기는 `SHARED_CONTEXT.md` 상단 규칙을 따른다.
- `[사용자]` 사용자가 직접 말한 것
- `[Claude 제안]` 아직 확정되지 않은 제안
- `[Codex 의견]` Codex가 남긴 것
- `[확정]` 합의되어 실행에 들어간 것
- `[측정]` 실제로 실행해서 얻은 수치

추측과 측정값을 섞어 적지 않는다. 측정 안 된 것은 측정 안 됐다고 적는다.

**Codex가 남긴 의견은 보존한다.** 동의하지 않더라도 지우거나 고쳐 쓰지 않는다.
반론이 있으면 `[Claude 제안]`으로 나란히 적는다.

## 이 프로젝트에서 특히 조심할 것

- **조용히 틀리는 버그가 이 프로젝트의 주된 실패 유형이다.** 크래시 없이 잘못된 수치가
  나온 사례가 여러 번 있고, 한 번은 22시간 학습을 통째로 날렸다.
  `SHARED_CONTEXT.md`의 버그 이력을 읽고, 긴 학습을 시작하기 전에 최소한 다음을 확인한다.
  - config diff에 의도한 차이만 있는가
  - 데이터가 실제로 그 값으로 들어오는가 (0이나 None으로 조용히 대체되지 않는가)
  - eval config가 학습 config와 구조적으로 일치하는가
- **규정 준수.** ego 상태(`ego_lcf`)와 목표점(`target_point`)은 planner 생성 단계에
  직접 넣을 수 없다. 진단용으로 위배 모델을 만들 때는 설정 파일 docstring에 제출 불가임을
  명시한다.
- **GPU는 사용자와 공유한다.** 3090에서 사용자가 직접 돌리는 작업이 있을 수 있다.
  지시 없이 중단하지 않는다.
- **학습 로그와 체크포인트 로딩 출력에 요약·필터를 걸지 않는다.** `rtk read -l aggressive`,
  `head`로 잘라 보기 등. 진단의 결정적 단서가 딱 한 줄인 경우가 반복해서 있었다
  (`size mismatch for ...`). 측정 근거는 `SHARED_CONTEXT.md` 인프라 메모의 RTK 항목.
- 학습 산출물은 컨테이너 안에서 root 소유라 호스트에서 지워지지 않는다. `docker exec`로 지운다.

## 현재 상태와 실행 규칙

`AGENTS.md`의 "지금 무엇이 도는가" / "긴 학습을 시작하기 전에" / "실행 스크립트" /
"데이터 경로 함정" 절이 Claude에도 그대로 적용된다. 두 파일의 내용이 갈리면
`AGENTS.md`와 `PIPELINE.md`가 기준이다.

요약:
- 전체 설계와 근거는 `PIPELINE.md`
- 긴 학습 전에 `tools/audit_pipeline.py` + `tools/check_accel_block_live.py` 필수
- 현재 계보는 `run_stage1_best.sh` / `run_stage2_best.sh` 뿐. 나머지는 폴백용 기록
- **shape가 맞아도 값이 안 흐를 수 있다.** 2026-09-12에 이 유형으로 세 건이 나왔다
